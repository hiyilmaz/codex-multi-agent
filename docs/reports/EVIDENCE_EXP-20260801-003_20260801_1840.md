# Evidence: EXP-20260801-003 Root-Only Contextual Voice Notification

Date: 2026-08-01 18:40 +03
Scope: Active global Codex Stop hook and project governance records

## Outcome

Status: passed
Success: true

The global Stop notification now speaks only for root Codex and selects one of
three approved messages:

- Completion: `Kodex işlemi tamamladı.`
- Failure: `Kodex bir hatayla karşılaştı.`
- Waiting for approval, an answer, or a decision:
  `Kodex senden yanıt bekliyor.`

Direct subagent events, Stop events backed by a subagent transcript, and
malformed hook input remain silent.

## Root And Subagent Evidence

Official Codex hook guidance separates `Stop` and `SubagentStop`, but the Stop
payload itself has no agent identifier. Existing local session records provide
the missing distinction:

- Root transcript source: `cli`
- Subagent transcript source:
  `session_meta.payload.source.subagent.thread_spawn`

The script uses that stable session-metadata distinction before classifying the
root assistant message.

## Test Evidence

RED inspection reported five missing capabilities in the old script.

GREEN dry-run cases:

```text
PASS completion -> Kodex işlemi tamamladı.
PASS failure -> Kodex bir hatayla karşılaştı.
PASS approval_wait -> Kodex senden yanıt bekliyor.
PASS question_wait -> Kodex senden yanıt bekliyor.
PASS subagent_event -> SILENT
PASS subagent_transcript -> SILENT
PASS malformed -> SILENT
PASS missing_message -> Kodex işlemi tamamladı.
GREEN=8_CASES_PASS
```

Every case returned `{"continue":true}`. Tests used
`CODEX_NOTIFY_TEST=1`, so no audio played. `zsh -n` passed and the active script
retained executable mode `0755`.

## Scope And Integrity

Changed runtime file:

- `/Users/iyilmaz/.codex/hooks/notify_stop.sh`

Unchanged and byte-verified:

- `/Users/iyilmaz/.codex/hooks.json`
- `/Users/iyilmaz/.codex/config.toml`

Hashes after implementation:

- Active script:
  `5e94937d42f9488d861a262c31dafb16c0ff11627db835c54c03b89783840fd5`
- Hook definition:
  `4f55b126fb8fc57688ae9b5aaebbcc8825c7972303d025a53128cf93675194f6`
- Global config:
  `2c45faa68c65e56fb1f71aecdd4e60fa2892a61289ead087897d2b1114fb6143`

## Rollback

Backup directory:

`/Users/iyilmaz/.codex/archive/voice-notify-20260801-184012/`

Previous script SHA-256:

`b2f422d486bdcd67a0a4c8e6410c0809d7448b8878aca09bd12152d04a0d8fba`

Restoration was not performed and requires an explicit rollback request.
