# Evidence — EXP-20260727-002

Date: 2026-07-27
Scope: Event-driven record archiving

## Implemented Contract

- Added `record-archive` to Codex, Dolphin, and active `~/.codex`.
- Deferred Findings: rotate at 10 Completed; retain all Pending plus 5
  Completed.
- Experiments: rotate at 10 terminal; retain all open plus 5 newest terminal.
- Changelog: rotate at 30 detailed dates or 500 lines when more than 20 dates
  exist; retain 20 full dates and link the next 30 archived dates.
- Checks run only on relevant record transitions or explicit requests.
- `check` is non-mutating; `apply` uses rollback-capable atomic file pairs.

## TDD Evidence

Initial RED:

```text
Ran 13 tests
FAILED (failures=15, errors=2)
```

Final focused suite:

```text
python3 -m unittest discover -s tests -p 'test_record_archive.py'
Ran 16 tests
OK
```

Full project suite:

```text
python3 -m unittest discover -s tests -p 'test_*.py'
Ran 35 tests
OK
```

Coverage:

```text
record_archive.py  252 statements  33 missed  87%
```

## Runtime Validation

`quick_validate.py` passed for:

- `variants/codex/home/skills/record-archive`
- `variants/dolphin/home/skills/record-archive`
- `/Users/iyilmaz/.codex/skills/record-archive`

Python compilation, executable-bit checks, Codex/Dolphin portable installation,
source/runtime checksums, and global policy equivalence passed.

The previous active global policy and registry files are recoverable from:

```text
/Users/iyilmaz/.codex/archive/record-archive-20260727_013000/
```

## Existing Project Compatibility

Read-only command:

```text
python3 record_archive.py check \
  --root /Users/iyilmaz/WebStorm/yedekparcasor.com \
  --record all
```

Result:

```text
deferred-findings: BELOW_THRESHOLD active=5 archive=26 moved=0
experiments: ACTION_REQUIRED active=5 archive=11 moved=11
changelog: BELOW_THRESHOLD active=11 archive=0 moved=0
```

No file under `yedekparcasor.com` was changed.

## Review

- Test integrity: behavior is exercised through the real CLI and temporary
  repositories; no production test branch or hardcoded success exists.
- Data safety: duplicate IDs/dates, wrong sections, unsupported headings,
  dirty managed files, and broken symlinks fail before writes.
- Transaction safety: active/archive pairs restore prior bytes if a write
  fails.
- Security impact: no dependency, network, authentication, API, database, or
  background-process change.

Decision: ACCEPT
