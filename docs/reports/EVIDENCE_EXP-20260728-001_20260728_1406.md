# Truthful Success Reporting Evidence

**Experiment:** EXP-20260728-001
**Date:** 2026-07-28 14:06 +03
**Scope:** Minimal Codex and Dolphin outcome-reporting policy, contract tests,
and reversible local Codex activation.

## Acceptance Contract

- Only `passed` may use `success=true`.
- `passed` requires actual execution, reviewed real output, satisfied criteria,
  no remaining critical failure, and concrete evidence.
- `failed`, `unverified`, and `not_executed` require `success=false`.
- The policy applies only to explicit task, operation, or test result reporting.
- Ordinary conversation has no mandatory status field or JSON format.

## RED

Command:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_orchestration_contract.py' -v
```

Actual result:

```text
Ran 10 tests in 0.981s
FAILED (failures=5)
Exit code: 1
```

The source and portable-install assertions failed because
`Truthful Success Reporting` was absent from all three runtime policies.

## GREEN And Regression

The first GREEN attempt still failed five assertions because a required phrase
crossed a Markdown line break. The test helper was corrected to normalize
whitespace while retaining every semantic marker.

Final focused result:

```text
Ran 10 tests in 1.375s
OK
```

Final full-suite result:

```text
Ran 37 tests in 7.716s
OK
```

The combined focused, full-suite, `git diff --check`, and Codex mirror command
returned exit code `0`. Portable Codex and Dolphin installations were exercised
by the focused suite.

## Independent Review

```text
code-reviewer: PASS
No assertion weakening, hardcoded success, skipped test, mock, test-only branch,
or scope violation found.

security-reviewer: NO_SECURITY_IMPACT
No fail-open behavior, authorization bypass, secret exposure, or weakened
control found.
```

## Active Runtime Activation

Read-only pre-check found that the active policy differed from the validated
source only by the new version, date, and truthful-reporting section.

Backup:

```text
/Users/iyilmaz/.codex/archive/truthful-success-before-20260728_140641/AGENTS.md
SHA-256: 34d846dc2db9c3d9f817da01bb4c199914ec243ff972220b0af07458340954ad
```

Validated source and active runtime:

```text
SHA-256: 882fc5dc6c62c59085a9da24ba279797e467d7301dcde9119cb1a16b2317cfe3
Mode: 0644
Activation exit code: 0
```

Rollback path:

```text
cp /Users/iyilmaz/.codex/archive/truthful-success-before-20260728_140641/AGENTS.md /Users/iyilmaz/.codex/AGENTS.md
cmp /Users/iyilmaz/.codex/archive/truthful-success-before-20260728_140641/AGENTS.md /Users/iyilmaz/.codex/AGENTS.md
```

Rollback was not executed because activation verification passed.

## Record And Final Checks

The event-driven archive check returned exit code `0`:

```text
deferred-findings: BELOW_THRESHOLD active=1 archive=0 moved=0
experiments: BELOW_THRESHOLD active=3 archive=0 moved=0
changelog: BELOW_THRESHOLD active=10 archive=0 moved=0
```

No archive apply was performed. The final full suite ran after the experiment,
changelog, and evidence updates:

```text
Ran 37 tests in 4.667s
OK
```

Final `git diff --check`, Codex source mirror comparison, and active runtime
comparison all returned exit code `0`.

## Result

Status: `passed`
Success: `true`
Reason: Every defined repository, review, installation, and active-runtime
criterion was executed and verified with concrete evidence.
