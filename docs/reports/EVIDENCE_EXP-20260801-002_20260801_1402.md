# Evidence: EXP-20260801-002 Medium-Only Subagent Matrix

Date: 2026-08-01 14:02 +03
Scope: This repository and `/Users/iyilmaz/.codex`

## Outcome

Status: passed
Success: true

All managed and active custom subagents now use `medium` reasoning. The model
matrix contains eight default roles and four Sol/medium routing variants. The
mandatory orchestration chain and security-review stage remain unchanged.

## Final Matrix

| Role | Default | Optional Sol route |
|---|---|---|
| planner | `gpt-5.6-terra` / `medium` | `planner-sol`, Sol/medium |
| tdd-guide | `gpt-5.6-terra` / `medium` | `tdd-guide-sol`, Sol/medium |
| code-reviewer | `gpt-5.6-sol` / `medium` | none |
| security-reviewer | `gpt-5.6-sol` / `medium` | none |
| explorer | `gpt-5.6-terra` / `medium` | `explorer-sol`, Sol/medium |
| docs-researcher | `gpt-5.6-terra` / `medium` | `docs-researcher-sol`, Sol/medium |
| reviewer | `gpt-5.6-sol` / `medium` | none |
| skill-agent-governor | `gpt-5.6-sol` / `medium` | none |

## Test Evidence

- RED: 10 focused tests; 7 failures and 1 missing-file error exposed the old
  high defaults, stale high variants, and missing Sol/medium variants.
- GREEN focused: 10 tests passed.
- GREEN full suite: 47 tests passed in 6.510 seconds.
- `git diff --check`: passed.
- Portable installation is covered by the focused contract suite.

## Active Runtime Evidence

A fresh `codex exec --strict-config --ephemeral --sandbox read-only` session,
with subagent spawning prohibited, returned:

```text
AGENT_FILES=12
REASONING_VALUES=medium
HIGH_VARIANTS=0
SOL_VARIANTS=docs-researcher-sol,explorer-sol,planner-sol,tdd-guide-sol
CHAIN=planner -> tdd-guide -> code-reviewer -> security-reviewer
SECURITY_NO_IMPACT=yes
```

All 12 managed agent files and the owned orchestration index, routing policy,
lazy module, and orchestration-gate skill are byte-equivalent to their active
counterparts. The active append-only audit log retains its independent history.

## Backup Evidence

Backup directory:
`/Users/iyilmaz/CodexBackups/20260801T135533-medium-only-subagents/`

- Repository ZIP SHA-256:
  `2471de99bea4981366760cd5cc770370b189eafd459e9db635898e907a6ce30b`
- Active Codex ZIP SHA-256:
  `67b5210a5e6fdee598a71e8d004479d83e7d6446b1d5a5e95c6f3c36cfb27890`
- `unzip -t`: passed for both archives.

## Project-Level Assessment

The scan under `/Users/iyilmaz/WebStorm` found zero project-local
`.codex/agents/*.toml` files pinning high reasoning. Project `AGENTS.md` and
project `.codex/` documents do not override this global subagent matrix, so no
project-level file change was required or made for EXP-20260801-002.

## Exclusions

No change was made to the main Codex session reasoning setting,
`/Users/iyilmaz/.codex/config.toml`, global `AGENTS.md`, auth, secrets, plugins,
Dolphin, remote hosts, or unrelated projects.
