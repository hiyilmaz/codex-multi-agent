# Evidence: EXP-20260804-001 CMA-EV Compatibility

Date: 2026-08-04 00:52 +03

## Claims

- CMA source requires an exact Claims section for new or materially updated evidence reports
- The CMA claim contract is preserved by the portable Codex installer
- EV validates the new CMA evidence format without weakening fail-closed parsing
- The revised real temporary EV to GLM pilot passed
- The active global CMA records module matches the validated source contract

## TDD Evidence

CMA source requires an exact Claims section for new or materially updated
evidence reports: the initial focused run failed two tests because the source
and installed module lacked the contract. The GREEN focused suite passed 12/12,
and the full CMA suite passed 49/49 with no failures or skips.

The CMA claim contract is preserved by the portable Codex installer: the
portable-install test created a temporary Codex runtime and asserted the full
contract in its installed `registry/modules/CMA_RECORDS.md`.

## EV Evidence

EV validates the new CMA evidence format without weakening fail-closed parsing:
the focused integration suite exercised the production `inspectEvidence` and
`parseResult` functions. Complete grounded coverage passed; omitted claims and
adversarial input returned `UNVERIFIED`. The final EV suite passed 34/34.

The revised real temporary EV to GLM pilot passed: after the first pilot exposed
GLM quoting claim declarations, validation guidance was strengthened to require proof
outside `## Claims` that supports the same claim. The subsequent end-to-end CLI
validation returned `PASS` with both claims grounded in the `## Proof` section.
The temporary project was disabled and moved to Trash.

## Review And Scope

Independent code review returned `PASS`. Independent security review returned
PASS with no findings. EV parser, schema, hooks, ACP client, dependencies, and
activation state were unchanged; only the validation prompt was strengthened.
No historical evidence report was rewritten for compatibility.

The active global CMA records module matches the validated source contract:
after explicit option A approval, the previous active file was copied to
`~/.codex/archive/cma-ev-claims-20260804_005548/CMA_RECORDS.md` with SHA-256
`3650620bc2e33097617ccb9df0c1ea068d0339e8bb3d2e33f8a4537affd3ca3f`.
The active and source files now both have SHA-256
`a381775d5e803d610a34b0b443a063baf029d9e3e4584703221cd353c4860028`,
and the active mode remains `0644`. No other global Codex file, real consumer
project, commit, push, or deployment was changed by this task.
