# Evidence: EXP-20260801-001

Date: 2026-08-01 03:15 Europe/Istanbul
Scope: This repository and `/Users/iyilmaz/.codex` only
Outcome: Accepted

## Scope And Safety

Included the managed Codex variant, repository governance/docs/tests, and the
approved active local Codex files. Excluded remote hosts, Hermes profiles,
other projects, Dolphin, `config.toml`, `auth.json`, secrets, and plugin state.
No commit or subagent spawn was performed.

## Rollback

- Repository and global ZIP archives were integrity-checked before activation.
- Targeted pre-activation snapshot:
  `/Users/iyilmaz/CodexBackups/20260801T235744Z-cma-core-lazy-active/`
- `shasum -a 256 -c manifest.sha256`: all listed files `OK`.
- The known incomplete archive
  `codex-home-global-20260731T230058Z.zip` is not a rollback source.

## RED To GREEN

- Initial RED: 5 tests, 21 failures. Failures covered missing modules, the
  483-line policy, unsupported `display_name`, absent escalation, and portable
  packaging gaps.
- Iterative RED checks caught implementation inserted into the mandatory chain,
  missing prior-context memory routing, unreliable spawn-time overrides, and
  ambiguous read-only precedence.
- Final focused result: 9 tests passed.
- Final full result:
  `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_*.py' -v`
  completed 46 tests in 8.785 seconds with `OK`.

## Implemented Runtime

- Core policy: 483 to 201 lines, 2,347 to 1,118 words, and 16,349 to
  8,443 bytes.
- Reduction: 58.4 percent lines, 52.4 percent words, 48.4 percent bytes.
- Eight lazy modules are packaged and active.
- The approved eight default roles retain their exact model/reasoning matrix.
- Six static high-risk variants pin `gpt-5.6-sol` / `high`: planner, TDD,
  code review, security review, exploration, and docs research.
- Static variants are required because agent-file values take precedence over
  parent or spawn model configuration.
- The exact mandatory chain remains
  `planner -> tdd-guide -> code-reviewer -> security-reviewer`.

## Runtime And Parity Checks

- Fresh Terra/medium session: exit 0, `CMA_RUNTIME_MEDIUM=ok`.
- Fresh Sol/high session: exit 0, `CMA_RUNTIME_HIGH=ok`.
- Final routing probe selected expected skip/ask/run decisions, lazy modules,
  exact chain, memory routing, and high-risk security escalation.
- Source and active copies match for the global policy, fourteen agent TOMLs,
  eight modules, orchestration registry/index, and orchestration-gate skill.
- Active audit history is intentionally append-only and therefore not replaced
  by the shorter portable source log.
- `display_name` search returned no standalone agent fields.

## Review

The first independent read-only review found four blocking gaps: unreliable
spawn overrides, marker-only escalation tests, ambiguous read-only precedence,
and stale plan/experiment records. The implementation replaced overrides with
static high variants, added RED/GREEN contracts, defined decision precedence,
and synchronized the records. Security-sensitive routing now selects
`security-reviewer-high`; the mandatory security stage remains non-skippable.
The scoped follow-up review returned `CODE_REVIEW: PASS` and
`SECURITY_REVIEW: NO_SECURITY_IMPACT`.

The terminal experiment triggered the required read-only archive check. It
returned `BELOW_THRESHOLD active=4 archive=0 moved=0`; no archive apply action
was needed.

## Decision

Accept Core CMA plus lazy modules and static high-risk routing. Roll back only
the owned active files from the verified targeted snapshot if a later runtime
regression is demonstrated.
