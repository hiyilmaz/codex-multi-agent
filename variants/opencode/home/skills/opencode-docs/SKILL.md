---
name: opencode-docs
description: Consult official OpenCode documentation for current configuration, MCP, skills, permissions, and CLI behavior.
---

# OpenCode Documentation

Use only official `opencode.ai/docs` sources for OpenCode behavior,
configuration, skills, permissions, MCP, and releases. Establish the installed
CLI version before applying version-specific configuration guidance.

Report the exact official source and distinguish verified facts from inferences.
Do not treat community content as authoritative. If official documentation or
the required CLI is unavailable, report `unverified` with `success=false`; do
not invent schema fields, credentials, or configuration changes.
