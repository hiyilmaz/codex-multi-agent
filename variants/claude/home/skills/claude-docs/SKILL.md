---
name: claude-docs
description: Consult official Claude Code documentation for current configuration, MCP, skills, permissions, and CLI behavior.
disable-model-invocation: true
user-invocable: true
---

# Claude Code Documentation

Use only official `code.claude.com/docs` or `docs.anthropic.com` sources for
Claude Code behavior, configuration, skills, permissions, MCP, and releases.

Report the exact official source and distinguish verified facts from inferences.
Do not treat community content as authoritative. If the official documentation
or required CLI is unavailable, report `unverified` with `success=false`; do
not invent schema fields, credentials, or configuration changes.
